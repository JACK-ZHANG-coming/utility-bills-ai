# 2026-08-08

- 部署"水电费快捷计算"项目到腾讯云服务器 (43.134.106.173)
- 服务器环境: Ubuntu 24.04, Nginx 1.24.0, 已有 Let's Encrypt SSL
- 生成 ed25519 SSH 密钥对，配置免密登录 (ssh root@43.134.106.173)
- 站点: https://aitools.zhangqiang.hk.cn/ → /var/www/aitools/dist/index.html
- 旧文件备份: /var/www/aitools/dist/index.html.bak

- 新增水电费记录列表功能：点击"保存数据"自动生成记录，支持手动增删改，按时间倒序排列，每页 5 条分页展示
- 记录字段包含：日期、用水量、水费单价、水费、用电量、电费单价、电费、总费用、备注
- 编辑/新增弹窗支持自动计算水费和电费合计
- 删除操作需确认弹窗，防止误删
- 记录数据存储键：`utility-bills-records-{roomId}`

- 修复删除记录 bug：`confirmDeleteRecord` 中 `closeConfirmDialog()` 会将 `pendingDeleteRoomId` 置 null，导致 `renderRecords(null)` 渲染空列表。修复方式：先缓存 roomId 到局部变量
- 修复 ID 生成：从 `Date.now()` 改为 `crypto.randomUUID()` 避免碰撞
- 修复 DOMContentLoaded 监听：脚本在 body 末尾，改为立即执行 IIFE
- 新增"抄表日期周期"字段：记录包含 `meterDateStart`/`meterDateEnd`，自动从表单 date1/date2 收集，弹窗可编辑，列表中以蓝色高亮条展示
- 新增"上期度数"/"本期度数"字段：记录包含 `elecReadingStart`/`elecReadingEnd`，自动从电费表单的 input1/input2 收集，弹窗可编辑，列表中以三列网格展示（抄表周期 | 上期度数 | 本期度数）
- 侧边栏默认收起：localStorage 未设置时默认 collapsed，需显式设为 false 才展开

## SEO 优化

- 优化 title 标签：从"水电费快捷计算"改为"水电费计算器 - 独立水表电表费用在线计算工具 | 免费快捷水电费计算"
- 新增 meta description / keywords / canonical / Open Graph / Twitter Card / robots 等元标签
- 新增 WebApplication + FAQPage 两种 JSON-LD 结构化数据
- 新增 apple-mobile-web-app 系列标签和 theme-color
- 页面底部新增 SEO 内容区域：工具介绍、核心功能、使用指南、适用场景、FAQ（5个问答）
- 改善语义化 HTML：subtitle 改为 p 标签，添加 aria-label 到 aside/main/a/svg
- 生成 seo-report.html SEO 优化报告
- 创建 robots.txt 和 sitemap.xml
- 文件从 2387 行增至 2628 行

## 工具介绍弹框组件

- 将底部 SEO 内容区域提取为独立弹框组件
- 在 header 右上角（GitHub 链接左侧）新增"工具介绍"按钮入口
- 弹框使用 `.intro-overlay` / `.intro-dialog` / `.intro-header` / `.intro-body` 类名前缀，避免与已有记录弹框 `.modal-overlay` 类名冲突
- 支持：点击关闭按钮、点击遮罩层、ESC 键三种关闭方式
- 弹框打开时 `body.style.overflow = 'hidden'` 防止背景滚动
- 动画：遮罩层淡入淡出 + 弹框缩放弹跳（cubic-bezier 弹性曲线）
- 移动端响应式：640px 以下缩小尺寸，380px 以下按钮文字隐藏仅留图标
- 弹框内容完整保留：工具介绍、核心功能、使用指南、适用场景、FAQ 5 个问答

- 标题从"水电费快捷计算"改为"水电费计算器"
- 生成网站图标（AI 生成 + sharp 多尺寸裁剪）：favicon.ico（16+32+48 多尺寸）、favicon-16/32/64/96 PNG、apple-touch-icon 180x180、icon-192/512 PWA 图标
- 修复 favicon 部署不显示问题：路径从相对（href="favicon-16x16.png"）改为绝对根路径（href="/favicon-16x16.png"），补充 favicon.ico 兜底，新增 PWA 图标引用
